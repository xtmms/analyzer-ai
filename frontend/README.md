# AI Log Analyzer — Frontend

React + Vite + TypeScript + Tailwind. Consuma l'API esposta da `backend/`.

Vedi il [README](../README.md) e [AGENTS.md](../AGENTS.md) nella root del repo per
setup, architettura e convenzioni.

```bash
cp .env.example .env
npm install
npm run dev
```
